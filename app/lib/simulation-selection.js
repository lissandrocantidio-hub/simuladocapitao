export function resolveEffectivePool(pool, priorityQuestionIds, questionCount) {
  // Retorna o pool completo e deixa a seleção fazer o trabalho
  // Não damos preferência absoluta para evitar sobrear-representação
  return pool
}
